use crate::{analyzer::AnalyzedProgram, symbol_table::SymbolTable};

//just serialize for now
pub fn compile(variables: &SymbolTable, analyzed_program: &AnalyzedProgram) -> Result<(), String> {
    unimplemented!()
}
